# Copy this file to .env for local development (docker-compose reads real env vars instead).
# In production (Render, etc.) set these as actual environment variables in the dashboard,
# never commit a real .env file.

MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=postarddb
APP_NAME=Postard
APP_URL=http://localhost:8080
APP_ENV=development
